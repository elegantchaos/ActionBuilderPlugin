# --------------------------------------------------------------------------------
# This workflow was automatically generated by ActionBuilderTool 2.0.1 (96).
# (see https://github.com/elegantchaos/ActionBuilderCore for more details)
# --------------------------------------------------------------------------------

name: Tests

on: [push, pull_request]

jobs:

    macOS-swift62:
        name: macOS (Swift 6.2)
        runs-on: macos-15
        steps:
        - name: Checkout
          uses: actions/checkout@v4
        - name: Install xcbeautify
          run: brew install xcbeautify
        - name: Make Logs Directory
          run: mkdir logs
        - name: Select Swift
          uses: beeauvin/swiftly-swift@v1
          with:
            swift-version: "6.2-snapshot"
        - name: Swift Version
          run: swift --version
        - name: Build (release)
          run: swift build -c release --quiet

